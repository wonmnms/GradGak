# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 자동으로 읽는 컨텍스트다.

## 프로젝트

광운대학교 졸업요건 분석 및 수강계획 시뮬레이터. 한 줄 소개·목표·Phase 로드맵은 [docs/project-plan.md](docs/project-plan.md), 상세 기능/데이터 요구사항은 [docs/requirements.md](docs/requirements.md) 참고.

## 문서 구조 (역할 겹치지 않게 유지할 것)

- `docs/project-plan.md`: 전략(배경·목표·Phase 로드맵·개발 원칙)
- `docs/requirements.md`: 상세 기능/데이터 요구사항 (현재 Phase 기준)
- 위 두 문서에 같은 내용을 양쪽에 다시 쓰지 말 것 — 한쪽에만 쓰고 다른 쪽에서는 링크로 참조
- `docs/meetings/YYYY-MM-DD.md`: 회의록. 양식은 `docs/meetings/template.md`. "Action Items"는 GitHub Issue로 만들기엔 작은 개인 준비 작업만 — 개발 작업은 Issue로 등록하고 링크만 남긴다
- 조사/제안 초안: repo가 아니라 Notion(“오픈소스SW개발 - 작업초안” 페이지)에서 관리한다. 결정되거나 폐기되면 해당 Notion draft는 지우고, 결과는 공식 문서(project-plan.md/requirements.md 등)와 회의록 결정사항에 반영
- `CONTRIBUTING.md`: 브랜치/커밋/PR 컨벤션

## AI 활용 원칙 (project-plan.md 7번과 동일)

- 졸업요건 판정 로직은 AI 추론에 의존하지 않고, 공식 학사자료 기반 명시적 규칙으로 처리한다
- 학사 규정/교육과정 원본 데이터를 구조화하는 데 AI를 보조 도구로 쓸 수 있으나, **AI가 추출·정리한 데이터는 반드시 원본 자료와 사람이 대조 검증한 뒤에만 판정 로직에 반영**한다
- 확인되지 않은 사실을 단정적으로 쓰지 말 것. URL을 추측해서 생성하지 말 것 — 검색/조회로 실제 확인된 것만 사용
- 데이터와 판정 로직은 분리해서 설계한다

## 확인된 핵심 도메인 사실 (매번 재조사하지 않도록 요약)

- 소프트웨어학부 졸업요건은 **정확히 4가지**: 세부전공필수교과목 이수, 졸업논문(작품) '가' 판정, TOPCIT 응시, 프로그램별(공학/일반) 졸업이수학점. 앞의 3개는 공학·일반 공통, 학점 요건만 프로그램별로 다름
- 입학 시 **전원 기본값이 공학프로그램**이며, 졸업하려면 **공학 요건 + 일반 요건을 동시에(AND)** 충족해야 한다(일반프로그램 전환은 4학년·7학기등록 등 특정 조건을 만족하는 예외적인 경우만 가능)
- C프로그래밍·스마트사회와인공지능(구.컴퓨팅사고 등) 계열 과목은 일반프로그램에서 전공 학점에 미포함 — 공학프로그램과 다름, 프로그램별로 과목의 전공 인정 여부가 갈릴 수 있음
- 상세 근거는 Notion [구현 가능성 및 난이도 검토](https://app.notion.com/p/3ddb38344e46816db958c43a93fdb349) 참고, 결정된 내용은 `docs/project-plan.md`/`docs/requirements.md` 참고

## Git

- push는 항상 사용자 확인을 받고 진행한다 (자동으로 push하지 않음)
- 개발 작업(코드 변경)은 `main`에 직접 커밋하지 않는다. `CONTRIBUTING.md`의 브랜치 전략(`type/짧은-설명`, 이슈 기반이면 이슈 번호 포함)에 따라 작업 브랜치를 만들고 PR로 병합한다. 문서 정리처럼 사소한 변경이 아니라면 브랜치 분리를 기본으로 한다
